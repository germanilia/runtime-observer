[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "runtime-observer"
version = "0.1.1"
description = "Runtime Observer Python SDK"
requires-python = ">=3.11"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["runtime_observer"]
